import React ,{ Component } from "react";
import {hot} from "react-hot-loader";

class Nomail extends Component{
    render(){
        return(
            <h1>Ningun mesaje seleccionado</h1>
        );
    }
}

export default hot(module)(Nomail);